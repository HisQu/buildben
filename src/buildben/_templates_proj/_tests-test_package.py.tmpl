"""Basic generated-package smoke coverage."""

from __future__ import annotations


def test_package_imports() -> None:
    """Keep the generated test suite non-empty and importable."""
    import {my_project}

    assert {my_project}.__name__ == "{my_project}"

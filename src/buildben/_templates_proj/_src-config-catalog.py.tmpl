"""Registered AppRC config metadata for <my_project>."""

import importlib

from <my_project>.config.app import APP_RC

# > Import the bundle so its section imports register every config declaration.
importlib.import_module("<my_project>.config.bundle")

CONFIG_SPEC = APP_RC.spec
CONFIG_SECTIONS = CONFIG_SPEC.owners
SECTION_BY_KEY = {section.key: section for section in CONFIG_SECTIONS}


def ensure_config_sections_registered() -> None:
    """Load every config declaration before AppRC creates a CLI schema.

    The config package facade stays lazy for normal imports. CLI construction
    calls this function so AppRC sees the complete section inventory.

    :return: None.
    """


__all__ = [
    "CONFIG_SECTIONS",
    "CONFIG_SPEC",
    "SECTION_BY_KEY",
    "ensure_config_sections_registered",
]
